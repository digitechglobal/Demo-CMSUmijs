import React from 'react';
import { FormProps } from 'antd/lib/form';
import { DrawerProps } from 'antd/lib/drawer';
import { Store } from 'antd/lib/form/interface';
import { CommonFormProps } from '../../BaseForm';
export declare type DrawerFormProps = Omit<FormProps, 'onFinish'> & CommonFormProps & {
    /**
     * @name 表单结束后调用
     * @description  接受返回一个boolean，返回 true 会关掉这个抽屉
     */
    onFinish?: (formData: Store) => Promise<boolean | void>;
    /**
     * @name 用于触发抽屉打开的 dom
     */
    trigger?: React.ReactNode;
    /**
     * @name 受控的打开关闭
     */
    visible?: DrawerProps['visible'];
    /**
     * @name 打开关闭的事件
     */
    onVisibleChange?: (visible: boolean) => void;
    /**
     * @name 抽屉的属性
     * @description 不支持 'visible'，请使用全局的 visible
     */
    drawerProps?: Omit<DrawerProps, 'visible'>;
};
declare const DrawerForm: React.FC<DrawerFormProps>;
export default DrawerForm;
