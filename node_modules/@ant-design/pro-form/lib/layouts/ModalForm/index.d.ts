import React from 'react';
import { FormProps } from 'antd/lib/form';
import { ModalProps } from 'antd/lib/modal';
import { Store } from 'antd/lib/form/interface';
import { CommonFormProps } from '../../BaseForm';
export declare type ModalFormProps = Omit<FormProps, 'onFinish'> & CommonFormProps & {
    /**
     * @name 表单结束后调用
     * @description  接受返回一个boolean，返回 true 会关掉这个弹窗
     */
    onFinish?: (formData: Store) => Promise<boolean | void>;
    /**
     * @name 用于触发抽屉打开的 dom
     */
    trigger?: React.ReactNode;
    /**
     * @name 受控的打开关闭
     */
    visible?: ModalProps['visible'];
    /**
     * @name 打开关闭的事件
     */
    onVisibleChange?: (visible: boolean) => void;
    /**
     * @name 弹框的属性
     * @description 不支持 'visible'，请使用全局的 visible
     */
    modalProps?: Omit<ModalProps, 'visible'>;
};
declare const ModalForm: React.FC<ModalFormProps>;
export default ModalForm;
